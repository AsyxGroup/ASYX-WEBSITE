"use client"
import { useState } from "react"
import { PageHero } from "@/components/page-hero"
import { Section } from "@/components/section"
import { quoteOptions, siteConfig } from "@/lib/data"
export default function RequestQuote(){const [form,setForm]=useState({name:"",organisation:"",email:"",phone:"",service:quoteOptions[0]||"",details:""});function submit(e:React.FormEvent){e.preventDefault();const subject=`ASYX consultation request - ${form.service}`;const body=`Name: ${form.name}\nOrganisation: ${form.organisation}\nEmail: ${form.email}\nPhone: ${form.phone}\nService: ${form.service}\n\nRequirement:\n${form.details}`;window.location.href=`mailto:${siteConfig.email}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`};return <><PageHero label="Request a consultation" title="Tell us what you need to achieve." subtitle="Share the scope, operational challenge or technology requirement and our team can continue the conversation with you." bgImage="/asyx/integration.jpg"/><Section><form onSubmit={submit} className="mx-auto max-w-3xl space-y-5 rounded-2xl border border-border bg-card p-6 sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium">Full name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="mt-2 h-12 w-full rounded-lg border border-input bg-background px-3"/></label><label className="text-sm font-medium">Organisation<input value={form.organisation} onChange={e=>setForm({...form,organisation:e.target.value})} className="mt-2 h-12 w-full rounded-lg border border-input bg-background px-3"/></label><label className="text-sm font-medium">Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="mt-2 h-12 w-full rounded-lg border border-input bg-background px-3"/></label><label className="text-sm font-medium">Phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} className="mt-2 h-12 w-full rounded-lg border border-input bg-background px-3"/></label></div><label className="block text-sm font-medium">Service / capability<select value={form.service} onChange={e=>setForm({...form,service:e.target.value})} className="mt-2 h-12 w-full rounded-lg border border-input bg-background px-3">{quoteOptions.map(o=><option key={o}>{o}</option>)}</select></label><label className="block text-sm font-medium">Requirement<textarea required rows={7} value={form.details} onChange={e=>setForm({...form,details:e.target.value})} className="mt-2 w-full rounded-lg border border-input bg-background p-3" placeholder="Describe the challenge, scope, location, timeline and any existing systems involved."/></label><button className="w-full rounded-lg bg-primary px-5 py-3.5 text-sm font-semibold text-primary-foreground">Prepare email to ASYX</button></form></Section></>}
