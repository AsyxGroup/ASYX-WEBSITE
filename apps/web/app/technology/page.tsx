import { PageHero } from "@/components/page-hero"
import { Section, SectionHeader } from "@/components/section"
import { RevealOnScroll } from "@/components/reveal-on-scroll"
import { QuoteCTA } from "@/components/quote-cta"
import { services } from "@/lib/data"
export const metadata={title:"Solutions & Systems Integration",description:"Integrated technology solutions from ASYX Group."}
export default function Technology(){const focus=services.filter(s=>["smart-technologies","telematics","cybersecurity","software","ict-infrastructure"].includes(s.slug));return <><PageHero label="Solutions" title="Technology that works as one environment." subtitle="ASYX combines platforms, infrastructure, connectivity, security, software and operational support around the outcome a client needs." bgImage="/asyx/integration.jpg"/><Section><RevealOnScroll><SectionHeader eyebrow="Integrated approach" title="Five capabilities at the heart of digital transformation."/></RevealOnScroll><div className="mt-10 space-y-5">{focus.map((s,i)=><RevealOnScroll key={s.slug} delay={i*60}><div className="grid overflow-hidden rounded-2xl border border-border bg-card md:grid-cols-[.8fr_1.2fr]"><img src={s.image} alt="" className="h-full min-h-56 w-full object-cover"/><div className="p-6 sm:p-8"><h2 className="text-2xl font-semibold">{s.title}</h2><p className="mt-3 leading-7 text-muted-foreground">{s.description}</p><div className="mt-5 flex flex-wrap gap-2">{s.items.map(x=><span key={x} className="rounded-full bg-muted px-3 py-1.5 text-xs font-medium">{x}</span>)}</div></div></div></RevealOnScroll>)}</div></Section><QuoteCTA/></>}
