import Link from "next/link"
import { ArrowRight, Phone } from "lucide-react"
import { RevealOnScroll } from "@/components/reveal-on-scroll"
export function QuoteCTA({ title="Let’s achieve together.", description="Tell us the operational challenge, technology requirement or infrastructure scope. ASYX can help design, deploy, operate and support the right solution." }: { title?: string; description?: string }) {
 return <section className="relative overflow-hidden bg-muted/40 py-14 sm:py-20"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,rgba(223,31,61,.12),transparent_30%),radial-gradient(circle_at_80%_80%,rgba(143,34,93,.12),transparent_30%)]"/><div className="relative mx-auto max-w-4xl px-4 text-center sm:px-6"><RevealOnScroll><span className="text-xs font-semibold uppercase tracking-[.22em] text-primary">Start a conversation</span><h2 className="mt-4 text-3xl font-semibold tracking-tight sm:text-4xl">{title}</h2><p className="mx-auto mt-5 max-w-2xl text-muted-foreground">{description}</p><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><Link href="/request-quote" className="inline-flex items-center justify-center gap-2 rounded-md bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Request a Consultation <ArrowRight className="size-4"/></Link><a href="tel:+255755432071" className="inline-flex items-center justify-center gap-2 rounded-md border border-border bg-background px-5 py-3 text-sm font-semibold"><Phone className="size-4"/>Call ASYX</a></div></RevealOnScroll></div></section>
}
